# React workspace

Structure only. React and TypeScript dependencies, compiler settings and packaging are deferred to Phase 4. Components are deferred to Phase 5. No exports or installable build exist. The workspace name is internal and does not reserve a registry scope.
